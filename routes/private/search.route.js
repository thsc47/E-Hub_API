const {Router} = require('express');

const searchRouter = Router();

myAccountRouter.get("/myaccount/:query", (req,res)=>{
    //TODO - Devolve a pesquisa baseada na query
})

module.exports = searchRouter